3. Kubernetes: 
	1) What are different types of services?
	   1.Cluster-IP  -- Default service in k8s
	   2.NodePort  
	   3.LoadBalancer
	   4.ExternalName
	2) What is a pod?
		Pod is small unit in kubernetes.
		Pod is a group of one or more container that are deploy together on the same host.
	
	3) Create a pod with the above created custom image when a pod dies k8s should automatically restart.
		Refer the repository 


	4) How to access the custom application with a specific port?
		Kubernetes Service and an associated Ingress or NodePort configuration. 
